#include <stdio.h>
int main() 
{
    int number;
    printf("Enter the number=");
    scanf("%d",&number);
    if(number>=0)
    printf("%d is a postive number",number);
    else
    printf("%d is a negative number", number);
     return 0;
}
