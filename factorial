#include<stdio.h>  
int main()    
{    
 int i,fact=1,number;   (NOTE;here fact 1 = the number multiplication stops at 1) 
 printf("Enter a number: ");  
  scanf("%d",&number);    
    for(i=1;i<=number;i++)
    {    
      fact=fact*i;    
  }    
  printf("Factorial of %d is: %d",number,fact);    
return 0;  
output:
nter a number: 5
Factorial of 5 is: 120
